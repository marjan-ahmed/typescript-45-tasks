function make_shirt(size: number, message: string) { //give the name to the module(procedure)

    console.log(`Making a ${size} t-shirt with the message "${message}" printed on it`);

}

make_shirt(26, "Coding is all about logic"); //call the procedure